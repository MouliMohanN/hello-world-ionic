# ConfigData

## Emulatore Files

### Categorizing the paths
```bash
        // Andy emulator files
        "fstab.andy",
        "ueventd.andy.rc",

        // Nox emulator files
        "fstab.nox",
        "init.nox.rc",
        "ueventd.nox.rc",
        "/system/bin/nox-prop",
        "/system/bin/noxd",
        "/system/lib/libnoxd.so",
        "/system/lib/libnoxspeedup.so",
        "/system/etc/init.nox.sh",
        "/system/bin/nox",
        "/system/bin/nox-vbox-sf",

        // Bluestacks emulator files
        "/system/priv-app/com.bluestacks.bstfolder.apk",
        "/dev/bst_gps",
        "/dev/bst_ime",
        "/dev/bstgyro",
        "/dev/bstmegn",
        "/data/.bluestacks.prop",
        "/data/.bstconf.prop",
        "/data/data/com.bluestacks.bstfolder",
        "/data/data/com.bluestacks.appmart",
        "/data/data/com.bluestacks.home",
        "/data/data/com.bluestacks.launcher",
        "/system/bin/bstfolder",
        "/system/bin/bstfolderd",
        "/system/bin/bstsyncfs",
        "/sys/module/bstsensor",
        "/sys/module/bstpgaipc",
        "/system/xbin/bstk/su",
        "/system/xbin/bstk",
        "/sys/module/bstpgaipc",
        "/system/xbin/bstk/su",
        "/system/xbin/bstk",

        // Phoenix OS emulator files
        "/system/xbin/phoenix_compat",
        "/data/system/phoenixlog.addr",
        "/system/phoenixos",

        // Memu emulator files
        "/dev/memufp",
        "/dev/memuguest",
        "/dev/memuuser",
        "/system/lib/memuguest.ko",

        // LDPlayer emulator files
        "/system/bin/ldinit",
        "/system/app/LDAppStore/LDAppStore.apk",
        "/data/data/com.ldmnq.launcher3/files/launcher.preferences",
        "/system/lib/hw/gps.ld.so",
        "/system/lib/hw/sensors.ld.so",
        "/system/lib/libldutils.so",

        // KoPlayer emulator files
        "/system/bin/KOPLAYER.ini",
        "/data/data/com.android.ld.appstore",
        "/data/data/com.koplay.launcher",

        // Droid4X emulator files
        "/system/bin/droid4x-vbox-sf",
        "/system/bin/droid4x",

        //  General emulator files
        "dev/socket/qemud",
        "dev/qemu_pipe",
        "/mnt/windows",
        "/mnt/windows/BstSharedFolder",
        "/lib/vboxguest.ko",
        "/lib/vboxsf.ko",
        "/sys/module/mg",
        "/sys/module/vboxpcismv",
        "/sys/module/nemusf",
        "/system/bin/genybaseband",
        "/sys/module/vboxsf",
        "/sys/module/msf",
        "/system/bin/androidVM-vbox-sf",
```

### AI generated paths
```bash
        // AI
        "/system/bin/androVM-vbox-sf", // AndroVM
        "/system/bin/genymotion", // Genymotion
        "/system/bin/genyd", // Genymotion
        "/system/bin/genyshell", // Genymotion
        "/system/bin/andy", // Andy
        "/system/bin/andy_x86", // Andy
        "/system/bin/andy_x64", // Andy
        "/system/bin/andy_vbox", // Andy
        "/system/bin/andy_vbox86", // Andy
        "/system/bin/andy_vbox64", // Andy
        "/system/bin/andy_vbox_x86", // Andy
        "/system/bin/andy_vbox_x64", // Andy

        "/system/bin/microvirt-prop", // MEmu
        "/system/bin/microvirtd", // MEmu
        "/system/lib/libdroid4x.so", // Droid4X
        "/system/lib/libdroid4xutils.so", // Droid4X
        "/system/bin/droid4x-prop", // Droid4X
        "/system/bin/droid4x-vbox", // Droid4X
        "/system/bin/droid4x-vbox86", // Droid4X
        "/system/bin/droid4x-vbox64", // Droid4X
        "/system/bin/droid4x-vbox_x86", // Droid4X
        "/system/bin/droid4x-vbox_x64", // Droid4X

        "/system/bin/ld-mount", // LDPlayer
        "/system/bin/ld-mount-prop", // LDPlayer
        "/system/bin/ld-mountd", // LDPlayer
        "/system/bin/ld-mountd-prop", // LDPlayer
        "/system/bin/ld-mountd-vbox", // LDPlayer
        "/system/bin/ld-mountd-vbox86", // LDPlayer
        "/system/bin/ld-mountd-vbox64", // LDPlayer
        "/system/bin/ld-mountd-vbox_x86", // LDPlayer
        "/system/bin/ld-mountd-vbox_x64", // LDPlayer   
        
        "/system/bin/ttVM_x86", // Tencent
        "/system/bin/ttVM_x64", // Tencent
        "/system/bin/ttVM_vbox86", // Tencent
        "/system/bin/ttVM_vbox64", // Tencent
        "/system/bin/ttVM_vbox_x86", // Tencent
        "/system/bin/ttVM_vbox_x64", // Tencent
```


## Products

```bash
        // AI
        "genymotion",
        "vbox86",
        "vbox",
        "vbox86tp",
        "vbox86t",
        "ttVM_Hdragon", // Tencent
        "ttVM", // Tencent
        "ldplayer",
        "bluestacks",
        "noxplayer",
        "memu",
        "koplayer",
        "phoenixos",
        "primeos",
        "remixos",
        "amiduos",
        "android_x86",
        "android_vm",
        "android_emulator",
        "android_simulator",
        "android_sdk",
        "android_dev",
        "android_test",
        "android_debug",
        "android_virtual",
        "android_device",
        "android_machine",
        "android_system",
        "android_platform",
        "android_environment",
        "android_instance",
        "android_container",
        "android_image",
        "android_snapshot",
```


## Manufacturers

```bash
        // AI
        "genymotion",
        "memu",
        "bluestacks",
        "ldplayer",
        "koplayer",
        "tencent",
        "ami_duos",
        "prime_os",
        "phoenix_os",
        "remix_os",
        "android_x86",
        "virtualbox",
        "vmware",
        "qemu",
        "parallels",
        "hyperv",
        "alps",
        "asusTek",
        "bignox",
        "emulator",
        "fake",
        "generic",
```



## DeviceInfo

```bash
        "generic_x86",
        "generic_x86_64",
        "generic_arm",
        "generic_arm64",
        "generic_mips",
        "generic_mips64",
        "genymotion",
        "memu",
        "bluestacks",
        "ldplayer",
        "koplayer",
        "tencent",
        "ami_duos",
        "prime_os",
        "phoenix_os",
        "remix_os",
        "android_x86",
        "virtualbox",
        "vmware",
        "qemu",
        "parallels",
        "hyperv",
        "alps",
        "asusTek",
        "bignox",
        "emulator",
        "fake",
        "generic",
```


## Hardwares

```bash
        // AI
        "vbox64",
        "ttVM_Hdragon", // Tencent
        "ttVM_x86", // Tencent
        "ttVM_x64", // Tencent
        "ttVM_vbox86", // Tencent
        "ttVM_vbox64", // Tencent
        "ttVM_vbox_x86", // Tencent
        "ttVM_vbox_x64", // Tencent
        "ranchu", // Android Emulator
        "qemu", // QEMU-based emulators
        "vbox", // VirtualBox
        "vbox86t", // VirtualBox
        "vbox86tp", // VirtualBox
```


# New methods generated by AI

## Check for Emulator-Specific Properties

```c++
void EmulatorDetection::checkEmulatorProperties() {
    std::vector<std::string> emulatorProperties = {
        "ro.kernel.qemu",
        "ro.product.model",
        "ro.product.brand",
        "ro.product.device",
        "ro.hardware",
        "ro.bootloader",
        "ro.build.characteristics"
    };

    for (const std::string& property : emulatorProperties) {
        std::string value = FileHelper::getSystemProperty(property);
        if (value.find("generic") != std::string::npos || value.find("unknown") != std::string::npos) {
            detections.push_back("- Detected Emulator Property: " + property + " = " + value);
        }
    }
}
```

## Check for Emulator-Specific Files

```c++
void EmulatorDetection::checkEmulatorFiles() {
    std::vector<std::string> emulatorFiles = {
        "/system/bin/qemu_props",
        "/system/bin/qemud",
        "/system/bin/qemu-props",
        "/system/lib/libc_malloc_debug_qemu.so",
        "/sys/qemu_trace",
        "/system/bin/androVM-prop",
        "/system/bin/microvirt-prop",
        "/system/bin/microvirtd",
        "/system/bin/nox-prop",
        "/system/bin/ttVM_x86",
        "/system/bin/ttVM_x64"
    };

    for (const std::string& file : emulatorFiles) {
        if (FileHelper::fileExists(file)) {
            detections.push_back("- Detected Emulator File: " + file);
        }
    }
}
```

## Check for Emulator-Specific Network Interfaces

```c++
void EmulatorDetection::checkEmulatorNetworkInterfaces() {
    std::vector<std::string> networkInterfaces = {
        "eth0",
        "wlan0"
    };

    for (const std::string& interface : networkInterfaces) {
        std::string result = FileHelper::executeCommand("ifconfig " + interface);
        if (result.find("00:16:3e") != std::string::npos || result.find("52:54:00") != std::string::npos) {
            detections.push_back("- Detected Emulator Network Interface: " + interface);
        }
    }
}
```

## Check for Emulator-Specific Processes

```c++
void EmulatorDetection::checkEmulatorProcesses() {
    std::vector<std::string> emulatorProcesses = {
        "qemu",
        "androVM",
        "nox",
        "ttVM"
    };

    for (const std::string& process : emulatorProcesses) {
        std::string result = FileHelper::executeCommand("ps | grep " + process);
        if (!result.empty()) {
            detections.push_back("- Detected Emulator Process: " + process);
        }
    }
}
```

## Check for Emulator-Specific Sensors

```c++
void EmulatorDetection::checkEmulatorSensors() {
    std::vector<std::string> sensors = {
        "goldfish",
        "vbox86",
        "nox",
        "VM_x86"
    };

    for (const std::string& sensor : sensors) {
        std::string result = FileHelper::executeCommand("getprop | grep " + sensor);
        if (!result.empty()) {
            detections.push_back("- Detected Emulator Sensor: " + sensor);
        }
    }
}
```

## Check for Emulator-Specific Directories

```c++
void EmulatorDetection::checkEmulatorDirectories() {
    std::vector<std::string> emulatorDirectories = {
        "/data/bluestacks",
        "/data/nox",
        "/data/androVM",
        "/data/microvirt",
        "/data/ttVM"
    };

    for (const std::string& directory : emulatorDirectories) {
        if (FileHelper::directoryExists(directory)) {
            detections.push_back("- Detected Emulator Directory: " + directory);
        }
    }
}
```

## Check for Emulator-Specific System Properties

```c++
void EmulatorDetection::checkEmulatorSystemProperties() {
    std::vector<std::string> emulatorProperties = {
        "ro.kernel.qemu",
        "ro.product.model",
        "ro.product.brand",
        "ro.product.device",
        "ro.hardware",
        "ro.bootloader",
        "ro.build.characteristics"
    };

    for (const std::string& property : emulatorProperties) {
        std::string value = FileHelper::getSystemProperty(property);
        if (value.find("generic") != std::string::npos || value.find("unknown") != std::string::npos) {
            detections.push_back("- Detected Emulator System Property: " + property + " = " + value);
        }
    }
}
```

## Check for Emulator-Specific Device IDs

```c++
void EmulatorDetection::checkEmulatorDeviceIDs() {
    std::vector<std::string> emulatorDeviceIDs = {
        "000000000000000", // Default device ID for emulators
        "0123456789ABCDEF" // Another common device ID for emulators
    };

    std::string deviceID = FileHelper::getSystemProperty("ro.serialno");
    for (const std::string& id : emulatorDeviceIDs) {
        if (deviceID == id) {
            detections.push_back("- Detected Emulator Device ID: " + deviceID);
        }
    }
}
```

## Check for Emulator-Specific IMEI Numbers

```c++ 
void EmulatorDetection::checkEmulatorIMEINumbers() {
    std::vector<std::string> emulatorIMEINumbers = {
        "000000000000000", // Default IMEI for emulators
        "012345678912345" // Another common IMEI for emulators
    };

    std::string imei = FileHelper::getSystemProperty("ro.ril.oem.imei");
    for (const std::string& number : emulatorIMEINumbers) {
        if (imei == number) {
            detections.push_back("- Detected Emulator IMEI Number: " + imei);
        }
    }
}
```

## Check for Emulator-Specific Build Characteristics

```c++
void EmulatorDetection::checkEmulatorBuildCharacteristics() {
    std::string buildCharacteristics = FileHelper::getSystemProperty("ro.build.characteristics");
    if (buildCharacteristics.find("emulator") != std::string::npos) {
        detections.push_back("- Detected Emulator Build Characteristics: " + buildCharacteristics);
    }
}
```

## Check for Emulator-Specific Input Devices

```c++
void EmulatorDetection::checkEmulatorInputDevices() {
    std::vector<std::string> inputDevices = {
        "qemu",
        "vbox",
        "nox"
    };

    std::vector<std::string> devices = FileHelper::readFile("/proc/bus/input/devices");
    for (const std::string& device : devices) {
        for (const std::string& inputDevice : inputDevices) {
            if (device.find(inputDevice) != std::string::npos) {
                detections.push_back("- Detected Emulator Input Device: " + inputDevice);
                return;
            }
        }
    }
}
```

## Check for Emulator-Specific OpenGL Renderer

```c++
void EmulatorDetection::checkEmulatorOpenGLRenderer() {
    std::vector<std::string> rendererStrings = {
        "Bluestacks",
        "Genymotion",
        "VirtualBox",
        "VMware"
    };

    std::string renderer = FileHelper::executeCommand("dumpsys SurfaceFlinger | grep GLES");
    for (const std::string& rendererString : rendererStrings) {
        if (renderer.find(rendererString) != std::string::npos) {
            detections.push_back("- Detected Emulator OpenGL Renderer: " + rendererString);
            return;
        }
    }
}
```

## Check for Emulator-Specific Bluetooth Address

```c++
void EmulatorDetection::checkEmulatorBluetoothAddress() {
    std::vector<std::string> bluetoothAddresses = {
        "02:00:00:00:00:00", // Default Bluetooth address for emulators
    };

    std::string address = FileHelper::getSystemProperty("ro.boot.btmacaddr");
    for (const std::string& bluetoothAddress : bluetoothAddresses) {
        if (address == bluetoothAddress) {
            detections.push_back("- Detected Emulator Bluetooth Address: " + address);
            return;
        }
    }
}
```

## Check for Emulator-Specific Telephony Information

```c++
void EmulatorDetection::checkEmulatorTelephony() {
    std::string operatorName = FileHelper::getSystemProperty("gsm.operator.alpha");
    std::string operatorNumeric = FileHelper::getSystemProperty("gsm.operator.numeric");

    if (operatorName == "Android" || operatorNumeric == "310260") {
        detections.push_back("- Detected Emulator Telephony Information");
    }
}
```

## Check for Emulator-Specific Build Fingerprint

```c++
void EmulatorDetection::checkEmulatorBuildFingerprint() {
    std::string buildFingerprint = FileHelper::getSystemProperty("ro.build.fingerprint");

    if (buildFingerprint.find("generic") != std::string::npos || buildFingerprint.find("unknown") != std::string::npos) {
        detections.push_back("- Detected Emulator Build Fingerprint: " + buildFingerprint);
    }
}
```

## Check for Emulator-Specific Audio Output

```c++
void EmulatorDetection::checkEmulatorAudioOutput() {
    std::string audioOutput = FileHelper::executeCommand("dumpsys audio");

    if (audioOutput.find("goldfish") != std::string::npos || audioOutput.find("vbox") != std::string::npos) {
        detections.push_back("- Detected Emulator Audio Output");
    }
}
```

## Check for Emulator-Specific Camera Information

```c++
void EmulatorDetection::checkEmulatorCamera() {
    std::string cameraInfo = FileHelper::executeCommand("dumpsys media.camera");

    if (cameraInfo.find("Emulated") != std::string::npos || cameraInfo.find("VirtualBox") != std::string::npos) {
        detections.push_back("- Detected Emulator Camera Information");
    }
}
```

## Check for Emulator-Specific Storage Paths

```c++
void EmulatorDetection::checkEmulatorStoragePaths() {
    std::vector<std::string> storagePaths = {
        "/mnt/sdcard",
        "/mnt/shell/emulated",
        "/storage/emulated"
    };

    for (const std::string& path : storagePaths) {
        if (FileHelper::directoryExists(path)) {
            detections.push_back("- Detected Emulator Storage Path: " + path);
        }
    }
}
```


# New Utility functions

## The FileHelper::executeCommand 

#### function is likely a utility function that executes a shell command and returns the output as a string. Here is a possible implementation of this function:

```c++
#include <string>
#include <array>
#include <memory>
#include <stdexcept>

class FileHelper {
public:
    static std::string executeCommand(const std::string& command) {
        std::array<char, 128> buffer;
        std::string result;
        std::shared_ptr<FILE> pipe(popen(command.c_str(), "r"), pclose);
        if (!pipe) {
            throw std::runtime_error("popen() failed!");
        }
        while (fgets(buffer.data(), buffer.size(), pipe.get()) != nullptr) {
            result += buffer.data();
        }
        return result;
    }
};
```